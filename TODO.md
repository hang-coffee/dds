# TODO

> 本清单基于 `manual.md` 与 `doctor-sim/dev_specification.md` 整理。
> 当前状态：DOCTOR 工具链整体可用，基础 ISA、DFE、DDE、模拟器、汇编器、C 编译器、链接器和反汇编器都已具备；
> 设备侧 PIT / FB / UART / KBC / DISK 已按 `dev_specification.md` 实现，但仍有测试与细节需要补齐。

## 通用 / 工程

- [x] 恢复或新增根目录一键 `Makefile` / 脚本，统一构建 `doctor-sim`、`dasm`、`dcc`、`dlinker`、`dda`
- [x] 清理 Git 仓库中的目录迁移残留：移除旧顶层 `src/`、`tests/`、`tools/` 等已删除路径，保持工作区与目录结构一致
- [x] 同步各 README 与实际实现：`doctor-sim/README.md` 已更新 DISK/dasm 反汇编等状态
- [x] 增加一键端到端测试脚本：`run_tests.sh` 覆盖 dcc、dasm、doctor-sim、dlinker 等测试
- [ ] 完善 `manual.md` 与 `doctor-sim/dev_specification.md`，确保与模拟器、dasm、dcc 实现严格同步

## doctor-sim：CPU / 中断 / 内存

- [ ] 调试器功能可增强：增加断点、单步、反汇编当前指令、内存/设备查看等
- [ ] 补充 DFE/DDE 边界测试：NaN、Inf、±0、舍入模式、异常粘着位、FIE 中断
- [ ] 对照 `manual.md` 检查指令编码示例、寄存器定义、中断/异常/MPU/浮点语义是否与模拟器一致
- [ ] `device_reset_all` 仍为预留接口，可完善设备复位语义并增加复位测试
- [ ] MMIO 尚未进入当前设备规范；如未来要支持内存映射设备，应先扩展 `dev_specification.md`，再在 `mem.h` 访问路径中接入设备映射
- [ ] RTC 尚未出现在 `dev_specification.md` 与实现中；若计划支持，应先补设备规范、端口分配、IRQ 分配

## doctor-sim：设备层（按 dev_specification.md）

### PIT

- [ ] 为 `TU=11`（CPU 周期）模式补充更完整的确定性自动化测试
- [ ] `TU=00/01/10` 真实时间模式依赖墙钟，测试不可复现；建议提供固定 tick / 模拟倍速 / 测试专用时钟开关
- [ ] 补充 `SE` 单次触发、`IE` 中断使能、计数器重载、“计数器未写入过不触发”等边界测试
- [ ] 按端口宽度补测试：`0x10` 为 8 位控制寄存器、`0x11` 为 32 位计数器，窄读/窄写行为应明确并验证

### FB

- [ ] 已有 `test_fb.asm` / `run_display_test.sh`；可补充显存或调色板越界、未使能时刷新、端口宽高与 Display 不一致等边界测试
- [ ] 补充只读/只写端口行为测试（如 `0x24` 只写、`0x21`/`0x25` 读写）

### UART

- [ ] 已覆盖环回、状态、溢出、接收中断；可补充 `TX_DELAY` 发送时序、FIFO 满丢弃、W1C 清除、`EN=0` 时行为测试
- [ ] 统一内部注释与文档：`uart.c` 中部分注释仍写 “IRQ1”，实际按规范为 `IRQ4`
- [ ] 补充 `uart_inject_byte()` 的宿主注入测试或命令行输入入口

### KBC

- [ ] 已覆盖自测、命令字节、键盘命令、OBF、中断；可补充 `E0` 扩展键序列、FIFO 满、禁用时中断不请求、读 `0x1A` 清 OBF 等测试
- [ ] 修正 `kbc.h` 注释中“2 字节 FIFO”与规范/实现中的 16 字节 FIFO 不一致
- [ ] 考虑提供脚本化键盘输入文件，便于自动化测试按键流与 Shift 序列

### DISK

- [ ] **最重要**：按 `dev_specification.md` 第 5 节补完整自动化测试；规范末尾仍标注“完整测试待 DISK 实现后补充”
- [ ] 覆盖内存传输模式：`READ_SECTORS` / `WRITE_SECTORS` 的 `BUF_ADDR != 0` 批量传输
- [ ] 覆盖 PIO 模式：`BUF_ADDR == 0` 时按 `BYTE/WORD/DWORD` 读写 `DATA` 端口，并验证 `DRQ` 状态
- [ ] 覆盖 64 位 LBA、`COUNT=0`、`LBA+COUNT > NUM_SECTORS`、缓冲区越界、写保护、`CFG.EN=0` 等错误路径
- [ ] 增加 `IRQ5` 端到端测试：`CFG.IE=1` 时命令完成请求中断，读 `STATUS` 清 `IRQ` 并撤下中断
- [ ] 增加 `RESET` / `NOP` 命令测试
- [ ] 增加窄读/窄写（`BYTE/WORD/DWORD`）对 32 位 DISK 寄存器的行为测试，验证高位保留/高位读零

### 未列端口

- [ ] 按 `dev_specification.md` 总览：未列端口 `IN` 返回全 1、`OUT` 被忽略、设备不存在时报 `#II`；补充自动化测试覆盖

## dasm

- [ ] 表达式目前只支持 `+` / `-`，不支持括号、乘除、逻辑表达式等
- [ ] 前向标号参与 `RESB` 等尺寸类表达式时，pass1 按 0 处理，可能产生错误；应明确限制或实现多趟求值
- [ ] `DB` / `DW` / `DD` / `DQ` 只接受单个数据值，不支持列表形式（如 `DB 1,2,3`）
- [ ] 预处理会在字符串内剥离 `;` 后的内容，与常规汇编器行为不一致
- [ ] 可集成或复用 `dda` 的反汇编能力，提供从二进制反汇编回 DASM 的入口
- [ ] 错误诊断可继续增强：更精确的操作数类型、尺寸冲突、标号作用域提示
- [ ] **关键** 用标准，不带有任何GNU拓展的C99重写dasm

## dcc

- [ ] 预处理器不支持 `#if` / `#elif` 表达式、`#error`、可变参数宏、反斜杠续行
- [ ] 数组初始化列表 `int a[3] = {1,2,3}` 不支持
- [ ] `void *` 只能作为通用指针，不能解引用；指针与整数互转缺少严格检查
- [ ] `long` / `double` 等 8 字节可变参数尚未完全支持
- [ ] 不支持 `static` 文件内函数/变量
- [ ] 函数指针数组、返回函数指针的复杂声明符受限
- [ ] 结构体按值传参/返回不支持；位域、匿名结构体/联合体不支持
- [ ] 栈顶固定为 `0x300000`，局部变量总量受栈空间限制；可改为可配置栈顶
- [ ] 当前只做“正确翻译”，不做优化；后续可增加简单优化（如常量折叠、公共子表达式消除）
- [ ] 代码生成与调用约定文档可补充更多 long、浮点、ISR 的边界情况
- [ ] **关键** 用标准，不带有任何GNU拓展的C99重写dcc

## dlinker

- [ ] 仅支持最小 ELF 链接；可扩展更多重定位类型、段合并策略、符号表输出
- [ ] 对无重定位表的旧格式扫描回退仅作兼容，建议最终只保留 ELF 正规路径
- [ ] 可增加链接脚本 / 地址映射文件支持，便于复杂内存布局
- [ ] 测试可增加浮点、ISR、多文件全局数组等更接近实际程序的场景
- [ ] **关键** 用标准，不带有任何GNU拓展的C99重写dlinker

## dda

- [ ] 目前只支持 ELF32 relocatable 反汇编，可扩展支持普通二进制反汇编
- [ ] 可增加符号名解析、重定位注释、数据段反汇编输出
- [ ] 可支持 `-Ttext` / `-Tdata` 等地址参数，方便反汇编最终链接后的镜像
- [ ] 加强与 dasm 的兼容性回归测试，确保反汇编产物可再次被 dasm 汇编

## 文档 / 测试

- [ ] 对照 `manual.md` 检查指令编码示例、寄存器定义、中断/异常/MPU/浮点语义是否与模拟器一致
- [ ] 对照 `doctor-sim/dev_specification.md` 检查每个设备的端口、寄存器位、中断行为是否与实现一致
- [ ] 为 DISK、RTC（若加入）、MMIO（若加入）、SVC/IRET 增加系统级端到端测试
- [ ] 为 DFE/DDE 增加完整边界测试：NaN、Inf、±0、舍入模式、异常粘着位、FIE 中断
- [ ] 增加从 C 源码到模拟器运行的一键演示/教程，降低使用门槛
