# Devices Specification (DOCTOR 设备明细)

## 0. 总览

### 端口分配

| 端口 | 设备 | 说明 |
|---|---|---|
| `0x10`-`0x11` | PIT | 可编程中断定时器 |
| `0x14`-`0x18` | UART | 通用异步收发器（COM1 风格） |
| `0x1A`-`0x1B` | KBC | AT 兼容键盘控制器（PC 0x60/0x64 风格） |
| `0x20`-`0x25` | FB | 帧缓冲（经 Display 层渲染） |

未列出的端口无设备：`IN` 读返回全 1，`OUT` 写被忽略（设备不存在时报 #II）。

### IRQ 分配

| IRQ | 设备 | RIN2 位 | 中断号 |
|---|---|---|---|
| 0 | PIT（定时器） | bit0 | ISRB + 0 |
| 1 | KBC（键盘） | bit1 | ISRB + 1 |
| 4 | UART（COM1 标准） | bit4 | ISRB + 4 |

中断号 = ISRB（RIN3_CTRL bit23-16）+ IRQ。所有硬中断为边沿触发；KBC 的中断
请求在 `OBF` 保持期间为电平式（见第 4 节）。

---

## 1. PIT设备

可编程中断定时器，用于在指定 Tick 内触发 **IRQ0** 中断。

### 端口

| 端口 | 尺寸 | 名称 | 方向 | 功能 |
|---|---|---|---|---|
| `0x10` | 8位 | `PIT_CTRL` | 读/写 | 设置寄存器 |
| `0x11` | 32位 | `PIT_COUNT` | 读/写 | 计数器：读=当前值；写=设定新值（下一 Tick 更新） |

### 设置寄存器 `0x10`

| 位 | 功能 | 缩写 | 注释 |
|---|---|---|---|
| 7 | 计时器使能 | `TE` | 1=开始执行计时器流程 |
| 6 | 中断使能 | `IE` | 1=每一次计数器归零都发送一次 IRQ0 中断 |
| 5 | 单次触发使能 | `SE` | 1=触发后不再重装计数器（等待重新写入非 0）；0=每次归零后重装设定值继续计时 |
| 4-3 | 时间单位 | `TU` | `00`=微秒，`01`=毫秒，`10`=秒，`11`=CPU周期（每单位 Tick 减 1） |
| 2-0 | 保留 | Rsvd | 读为 0 |

### 行为

计时器流程：每 Tick 执行 `计数器--`；计数器归零时按 `IE` 发送 IRQ0。

计数器为 0 且已发送过中断（或 `IE=0`）时：

* `SE=0`：计数器复位为上一次设定的值，继续计时。
* `SE=1`：停止计时，直至重新写入非 0 计数器值后继续。

注意：TU=00/01/10（真实时间模式）由模拟器墙钟驱动，与执行速度无关，测试不可复现；
TU=11（CPU 周期模式）每指令一个 Tick，可用于确定性测试。

### 中断

计数器归零且 `IE=1` 时请求 **IRQ0**（RIN2 bit0，中断号 = ISRB + 0）。

### 硬件初始值

`0x10` = `0000_0000B`（未使能、无中断）；`0x11` = `0`；无挂起中断。

---

## 2. FB设备（帧缓冲）

帧缓冲设备，通过 **Display 层**（`src/display.h`）渲染。程序只需在数据区构造
显存（索引色）与调色板，配置端口并触发刷新；具体显示方式（终端 / PPM 图像文件 /
内存帧缓冲）由模拟器启动时的 `--display` 后端决定，设备与程序不关心。

### 端口

| 端口 | 尺寸 | 名称 | 方向 | 功能 |
|---|---|---|---|---|
| `0x20` | 8位 | `FB_CTRL` | 读/写 | 控制寄存器 |
| `0x21` | 32位 | `FB_BASE` | 读/写 | 显存基址（数据区地址，`宽*高` 字节索引色） |
| `0x22` | 16位 | `FB_WIDTH` | 读/写 | 帧缓冲宽度 |
| `0x23` | 16位 | `FB_HEIGHT` | 读/写 | 帧缓冲高度 |
| `0x24` | 8位 | `FB_REFRESH` | 只写 | 刷新命令（写入任意值 → 读取显存+调色板 → Display 渲染） |
| `0x25` | 32位 | `FB_PALETTE` | 读/写 | 调色板基址（数据区地址，每项 3 字节 RGB，最多 256 项） |

### 控制寄存器 `0x20`

| 位 | 功能 | 缩写 | 注释 |
|---|---|---|---|
| 7-1 | 保留 | Rsvd | 读为 0 |
| 0 | 帧缓冲使能 | `EN` | 1=使能。未使能时刷新命令被忽略 |

### 行为

刷新流程：写入 `0x24` 时，若 `EN=1` 且显存/调色板基址有效，设备从数据区读取
`宽*高` 字节索引色与 256×3 字节调色板，调用 `display_blit_indexed()` 填充
Display 帧缓冲并 `display_flush()`。实际渲染尺寸以 Display 帧缓冲为准
（由 `--display-size` 决定），端口中的宽高用于校验，不一致时给出警告并以 Display 为准。
显存/调色板超出数据空间时刷新被忽略（给出警告）。

### 中断

**无中断**（帧缓冲为被动显示设备，不产生 IRQ）。

### 硬件初始值

`0x20` = `0000_0000B`（未使能）；`0x21`/`0x25` = 0；`0x22`/`0x23` = 0。

### 示例（dasm 汇编）

```
	; 调色板 @ 0x3200: 索引0=黑, 1=红, 2=绿, 3=蓝
	; 显存   @ 0x3000: 8x8 索引色, (0,0)=红
	LET A, DWORD 0x3000
	OUT DWORD 0x21, A		; 显存基址
	LET A, DWORD 0x3200
	OUT DWORD 0x25, A		; 调色板基址
	LET A, WORD 8
	OUT WORD 0x22, A		; 宽
	OUT WORD 0x23, A		; 高
	LET A, BYTE 0x01
	OUT BYTE 0x20, A		; 使能
	OUT BYTE 0x24, A		; 刷新
```

（完整示例见 `tests/test_fb.asm`；PPM 后端断言见 `tests/run_display_test.sh`）

---

## 3. UART设备

通用异步收发器。接收侧为 16 字节 FIFO（数据来源：环回或宿主注入），发送侧为
单字节缓冲。输出字节交给 **Display 层**（字符输出），显示方式由 `--display` 后端
决定；输入字节可由环回模式或 `uart_inject_byte()` 注入。

### 端口

| 端口 | 尺寸 | 名称 | 方向 | 功能 |
|---|---|---|---|---|
| `0x14` | 8位 | `UART_RXD` | 只读 | 输入寄存器：读取接收 FIFO 的一个字节（空则返回 `0xFF`） |
| `0x15` | 16位 | `UART_RXST` | 读/写 | 输入状态：读=状态字；写=按位清除（W1C） |
| `0x16` | 8位 | `UART_TXD` | 只写 | 输出寄存器：写入即发送一个字节 |
| `0x17` | 16位 | `UART_TXST` | 读/写 | 输出状态：读=状态字；写=按位清除（W1C） |
| `0x18` | 32位 | `UART_CFG` | 读/写 | 配置寄存器 |

### 输入状态 `0x15`（读）

| 位 | 名称 | 含义 |
|---|---|---|
| 0 | `RX_READY` | 1=接收 FIFO 非空，可读 `0x14` |
| 1 | `RX_OVERFLOW` | 1=FIFO 满时又有数据到达（最新数据被丢弃）；写 1 到 `0x15` 清除 |
| 15-2 | Rsvd | 读为 0 |

### 输出状态 `0x17`（读）

| 位 | 名称 | 含义 |
|---|---|---|
| 0 | `TX_READY` | 1=发送缓冲空闲，可写 `0x16` |
| 1 | `TX_OVERFLOW` | 1=发送缓冲忙时写入被丢弃；写 1 到 `0x17` 清除 |
| 15-2 | Rsvd | 读为 0 |

### 配置 `0x18`

| 位 | 名称 | 含义 |
|---|---|---|
| 0 | `EN` | 1=使能。未使能时输入/输出/中断均不工作 |
| 1 | `IE` | 1=接收中断使能（接收数据就绪时请求 `IRQ4`，COM1 标准） |
| 2 | `LOOP` | 1=环回：发送的字节同时进入接收 FIFO |
| 7-3 | `TX_DELAY` | 发送每字节消耗的 tick 数（0=立即完成；1-31=发送进行中） |
| 31-8 | Rsvd | 保留 |

### 行为

* **发送**：写 `0x16` 时若 `TX_READY=1` 则开始发送——字节交给 Display 层输出，
  环回模式下同时进入接收 FIFO；`TX_READY` 变为 0，持续 `TX_DELAY` 个 tick 后恢复 1。
  若 `TX_READY=0`（上一字节未完成），写入被丢弃并置 `TX_OVERFLOW`。
* **接收**：FIFO（16 字节）非空时 `RX_READY=1`；满时新数据被丢弃并置
  `RX_OVERFLOW`。读 `0x14` 弹出队首字节。
* **中断**：接收 FIFO 从空变为非空且 `EN=1`、`IE=1` 时请求 **IRQ4**
  （COM1 标准；RIN2 bit4，中断号 = ISRB + 4）。中断处理由 CPU 按 ICT/INL/IPL 规则进行。
* **宿主注入**：`uart_inject_byte(cpu, b)` 把一个字节放入接收 FIFO
  （供宿主程序/未来输入设备使用），等效于外部串口收到一个字节。

### 硬件初始值

`0x18` = 0（未使能）；FIFO 空；状态为 0；无中断。

### 示例（dasm 汇编）

```
	; 环回自测: 配置 EN|LOOP, 写 'A' 再读回
	LET A, DWORD 0x00000005		; EN=1, LOOP=1
	OUT DWORD 0x18, A
	LET A, BYTE 0x41			; 'A'
	OUT BYTE 0x16, A			; 发送（同时进入接收 FIFO）
	IN BYTE 0x14, D1			; 读回 0x41
```

（完整示例与中断流程见 `tests/test_uart.asm`）

---

## 4. KBC设备（AT 兼容键盘控制器）

IBM PC/AT 兼容的键盘控制器（8042/PS/2 风格）。键盘**默认且仅支持扫描码集
Set 1**（不提供 Set 2）。端口映射与 PC 一致：`0x1A` ↔ PC `0x60`（数据端口），
`0x1B` ↔ PC `0x64`（状态/命令端口）。

### 端口

| 端口 | 尺寸 | 名称 | 方向 | 功能 |
|---|---|---|---|---|
| `0x1A` | 8位 | `KBC_DATA` | 读/写 | 读=输出缓冲（扫描码/控制器响应）；写=发送键盘命令或命令字节数据 |
| `0x1B` | 8位 | `KBC_STAT_CMD` | 读/写 | 读=状态寄存器；写=8042 命令 |

### 状态寄存器 `0x1B`（读）—— 8042 标准位

| 位 | 名称 | 含义 |
|---|---|---|
| 0 | `OBF` | 输出缓冲满：`0x1A` 有数据可读（读 `0x1A` 后清 0） |
| 1 | `IBF` | 输入缓冲满：控制器忙（本模拟器写入瞬时完成，恒为 0） |
| 2 | `SYS` | 系统标志：自测（`0xAA`）通过后置 1，不复位 |
| 3 | `C_D` | 命令/数据标志：最近一次写入 `0x1B`（命令）=1，写入 `0x1A`（数据）=0 |
| 4 | `KBD_INH` | 键盘禁用：0=禁用（命令字节 bit3=1），1=启用 |
| 7-5 | Rsvd | 读为 0 |

### 8042 命令（写 `0x1B`）

| 命令 | 功能 | 响应（进入输出缓冲） |
|---|---|---|
| `0xAA` | 自测 | `0x55`（同时 SYS=1） |
| `0xAB` | 接口测试 | `0x00` |
| `0xAD` | 禁用键盘 | 无（命令字节 bit3=1，状态 bit4=0） |
| `0xAE` | 启用键盘 | 无（命令字节 bit3=0，状态 bit4=1） |
| `0x20` | 读命令字节 | 当前命令字节 |
| `0x60` | 写命令字节 | 无（随后一次写 `0x1A` 为命令字节值，此时不视为键盘命令） |
| `0xFE` | 复位 | `0xFA` |

未知命令被忽略（状态 bit3 仍置 1）。

### 命令字节（8042 配置字节，经 `0x20`/`0x60` 读写）

| 位 | 含义 |
|---|---|
| 0 | 键盘中断使能：1=输出缓冲就绪时请求 `IRQ1` |
| 1 | 鼠标中断使能（本设备无鼠标，保留） |
| 2 | 系统标志（软件可读写；与状态 bit2 独立） |
| 3 | 键盘禁用：1=禁用（状态 bit4=0，扫描码中断不再请求） |
| 6 | 翻译模式（本设备输入恒为 Set 1，仅记录） |

### 键盘命令（写 `0x1A`，非"写命令字节"等待状态时）

| 命令 | 功能 | 响应 |
|---|---|---|
| `0xEE` | 回显 | `0xEE` |
| `0xED` | 设置 LED | `0xFA`（ACK） |
| `0xF0` | 读/选扫描码集 | `0xFA` + `0x01`（本设备为 Set 1） |
| `0xF4` | 启用 | `0xFA` |
| `0xF5` | 禁用 | `0xFA` |
| `0xFF` | 复位 | `0xFA` + `0xAA`（自检通过） |
| 其它 | 未知命令 | `0xFA` |

### 扫描码注入

宿主程序调用 `kbc_inject_scancode(cpu, byte)` 把一个 Set 1 扫描码字节放入输出缓冲
（16 字节 FIFO；等效于键盘事件流）。make 码、break 码（`0x80|码`）与 `0xE0` 前缀
序列均可逐字节注入（如 `0xE0`、`0x48`、`0xE0`、`0xC8` = 上方向键按下、松开）。
输出缓冲满时注入失败（返回 false，宿主可稍后重试）。

模拟器内置键盘输入（`src/input.c`，stdin 为终端时启用）按按键事件流注入：
普通字符注入 make + break；大写/上档字符先注入 `0x2A`（LShift make），再注入
键的 make + break，最后 `0xAA`（LShift break），供程序跟踪 Shift 状态。

### 中断

输出缓冲就绪（`OBF=1`）且命令字节 bit0=1 且未禁用（bit3=0）时请求 **IRQ1**
（PC 标准；RIN2 bit1，中断号 = ISRB + 1）。中断为**电平式**：`OBF` 保持期间每个
tick 持续请求（ISR 必须读 `0x1A` 清空输出缓冲以解除中断）。

### 硬件初始值

命令字节 `0x00`（无中断、键盘启用）；状态 `0x00`（SYS=0、C_D=0、KBD_INH=1）；
输出缓冲空。键盘工作于 Set 1。

### Set 1 扫描码表（完整，make / break）

break 码 = make 码 | `0x80`。`E0` 前缀序列的 break = `E0` +（第二字节 | `0x80`）。

| 键 | make | break | 键 | make | break |
|---|---|---|---|---|---|
| Esc | 01 | 81 | `1` | 02 | 82 |
| `2` | 03 | 83 | `3` | 04 | 84 |
| `4` | 05 | 85 | `5` | 06 | 86 |
| `6` | 07 | 87 | `7` | 08 | 88 |
| `8` | 09 | 89 | `9` | 0A | 8A |
| `0` | 0B | 8B | `-` | 0C | 8C |
| `=` | 0D | 8D | Backspace | 0E | 8E |
| Tab | 0F | 8F | Q | 10 | 90 |
| W | 11 | 91 | E | 12 | 92 |
| R | 13 | 93 | T | 14 | 94 |
| Y | 15 | 95 | U | 16 | 96 |
| I | 17 | 97 | O | 18 | 98 |
| P | 19 | 99 | `[` | 1A | 9A |
| `]` | 1B | 9B | Enter | 1C | 9C |
| LCtrl | 1D | 9D | A | 1E | 9E |
| S | 1F | 9F | D | 20 | A0 |
| F | 21 | A1 | G | 22 | A2 |
| H | 23 | A3 | J | 24 | A4 |
| K | 25 | A5 | L | 26 | A6 |
| `;` | 27 | A7 | `'` | 28 | A8 |
| `` ` `` | 29 | A9 | LShift | 2A | AA |
| `\` | 2B | AB | Z | 2C | AC |
| X | 2D | AD | C | 2E | AE |
| V | 2F | AF | B | 30 | B0 |
| N | 31 | B1 | M | 32 | B2 |
| `,` | 33 | B3 | `.` | 34 | B4 |
| `/` | 35 | B5 | RShift | 36 | B6 |
| KP `*` | 37 | B7 | LAlt | 38 | B8 |
| Space | 39 | B9 | CapsLock | 3A | BA |
| F1 | 3B | BB | F2 | 3C | BC |
| F3 | 3D | BD | F4 | 3E | BE |
| F5 | 3F | BF | F6 | 40 | C0 |
| F7 | 41 | C1 | F8 | 42 | C2 |
| F9 | 43 | C3 | F10 | 44 | C4 |
| NumLock | 45 | C5 | ScrollLock | 46 | C6 |
| KP7/Home | 47 | C7 | KP8/Up | 48 | C8 |
| KP9/PgUp | 49 | C9 | KP `-` | 4A | CA |
| KP4/Left | 4B | CB | KP5 | 4C | CC |
| KP6/Right | 4D | CD | KP `+` | 4E | CE |
| KP1/End | 4F | CF | KP2/Down | 50 | D0 |
| KP3/PgDn | 51 | D1 | KP0/Ins | 52 | D2 |
| KP`.`/Del | 53 | D3 | SysRq | 54 | D4 |
| (未分配) | 55 56 | D5 D6 | F11 | 57 | D7 |
| F12 | 58 | D8 | (未分配) | 59-7F | D9-FF |

`E0` 前缀扩展键（make 序列 = `E0` + 下表第二字节；break = `E0` + 下表 break）：

| 键 | 第二字节 | break | 键 | 第二字节 | break |
|---|---|---|---|---|---|
| KP Enter | 1C | 9C | RCtrl | 1D | 9D |
| KP `/` | 35 | B5 | RAlt | 38 | B8 |
| Home | 47 | C7 | Up | 48 | C8 |
| PgUp | 49 | C9 | Left | 4B | CB |
| Right | 4D | CD | End | 4F | CF |
| Down | 50 | D0 | PgDn | 51 | D1 |
| Ins | 52 | D2 | Del | 53 | D3 |
| LGUI（Win） | 5B | DB | RGUI（Win） | 5C | DC |
| Apps（菜单） | 5D | DD | Power | 5E | DE |
| Sleep | 5F | DF | Wake | 63 | E3 |

### 示例（dasm 汇编）

```
	; 自测: 写 0x1B=0xAA → 读 0x1A=0x55
	LET A, BYTE 0xAA
	OUT BYTE 0x1B, A
	IN BYTE D1, 0x1A			; D1 = 0x55
```

（完整流程与中断测试见 `tests/test_kbc.asm`）
