# Devices Specification (DOCTOR 设备明细)

## 1. PIT设备

可编程中断定时器，用于在指定Tick内触发IRQ0中断。

### 端口

* `0x10` - 8位，PIT设置寄存器
* `0x11` - 32位，PIT计数器

对于PIT设置寄存器：

| 位 | 功能 | 缩写 | 注释 |
|---|---|---|---|
| 7 | 计时器使能 | `TE` | 用于控制是否开启PIT计时功能。如果这一位为1，那么开始执行计时器流程。 |
| 6 | 中断使能 | `IE` | 用于控制是否发送硬中断。如果这一位为1，那么每一次计数器归零，都会发送一次IRQ0中断。 |
| 5 | 单次触发使能 | `SE` | 用于控制是否采用单次触发模式。如果这一位为1，那么触发中断后不再重新设置计数器。如果这一位为0，那么每次重新触发中断后，都会把寄存器归位为设定值，继续计时。 |
| 4-3 | 时间单位 | `TU` | 用于控制Tick的单位。`00`表示微秒，`01`表示毫秒，`10`表示秒，`11`表示CPU周期。（也就是每微秒/毫秒/秒/Cycle，Tick减少1） |
| 2-0 | 保留 | `Rsvd` | / |

对于PIT计数器，读取时用于获得目前计数器的值。写入时用于设定计数器的新值，在下一Tick内更新。

计时器流程：在每Tick内进行`PIT计数器--`的操作。如果PIT计数器=0则根据`IE`的设定发送中断。

如果计数器为0，且已经发送过中断/`IE=0`，则：

* 如果`SE=0`，就将计数器复位为上一次设定的值。继续进行计时器流程。
* 如果`SE=1`，就不进行任何操作，直至重新设定计数器的值为非0。那之后继续进行计时器流程。

#### 硬件初始值

`0x10`端口设置`为0000_0000B`，`0x11`端口设置为`0`。

